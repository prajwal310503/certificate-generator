## Running the Project

Open two terminals.

### Terminal 1 — Backend

```bash
cd backend
npm install
npm run dev
```

Runs on `http://localhost:5000`

### Terminal 2 — Frontend

```bash
cd frontend
npm install
npm run dev
```

Runs on `http://localhost:5173`

Open `http://localhost:5173` in your browser.

